import { RouterModule, Routes } from '@angular/router';

/*

import {MaincontentComponent} from './maincontent/MasterNumber/maincontent-component.component';
import { DashboardComponent } from './maincontent/dashboard/dashboard.component';

const routes: Routes = [
      { path: '', redirectTo: '/home', pathMatch: 'full' },
      { path: 'Dashboard',  component: DashboardComponent,},
      { path: ':MasterNumber', component: MaincontentComponent }
    ];

*/
export class appsRouting {
}
